<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#">WIDGET GALLERY</a>

  <div class="collapse navbar-collapse" id="navbarText">
    <ul class="navbar-nav mr-auto"></ul>
    <span class="navbar-text">
       <%= link_to "LOGIN", login_path, remote: true, class:'btn btn-primary'  unless logged_in? %>
       <%= link_to "LOGOUT", logout_path, method: :delete, class:'btn btn-outline-primary' if logged_in? %>
    </span>
  </div>
</nav>